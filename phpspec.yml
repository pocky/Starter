extensions:
  - Memio\SpecGen\MemioSpecGenExtension
  - RMiller\BehatSpec\PhpSpecExtension

suites:
    application_suite:
        namespace: Application
        src_path: %paths.config%/app/src
        spec_path: %paths.config%/app

    domain_suite:
        namespace: Domain
        src_path: %paths.config%/app/src
        spec_path: %paths.config%/app

    infrastructure_suite:
        namespace: Infrastructure
        src_path: %paths.config%/app/src
        spec_path: %paths.config%/app
